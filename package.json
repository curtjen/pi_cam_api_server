{
  "name": "picam_server",
  "version": "1.0.0",
  "description": "API server to control your camera using gphoto2.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "curtjen",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1"
  }
}
